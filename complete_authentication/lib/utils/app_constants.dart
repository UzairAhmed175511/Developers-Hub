import 'package:flutter/material.dart';

class AppConstants {
  // Colors
  static const primaryColor = Colors.blue;
  static const secondaryColor = Colors.white;

  // Padding
  static const double defaultPadding = 16.0;

  // Strings
  static const appName = 'Complete Authentication';
}
